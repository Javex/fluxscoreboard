You have registered for the hack.lu ${year} CTF with your Team ${team.name}. To confirm your registration, pleace click this link:
<a href="${request.route_url('confirm', token=team.token)}">${request.route_url('confirm', token=team.token)}</a>
